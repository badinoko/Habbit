# Progress

## 2026-03-21

- Added initial assistant workflow files: `AGENTS.md`, `CODEX.md`, `docs/operations/codex_workflow.md`, `docs/project/startup.md`, `docs/project/overview.md`.
- Recorded that `C:\Users\user\Projects\HabitFlow-master` is currently a local snapshot without `.git`.
- Seeded the initial lab task list for bootstrap, runtime baseline, frontend audit, OAuth audit, and implementation wave planning.
- Imported a Besedka redesign reference pack into `docs/imports/besedka_design_kit_2026-03/` with core UI references, redesign artifacts, prompts, homepage context, screenshots, and dense gallery/growreports reference packs.
- No task was marked `DONE`.
- Cursor accidentally initialized `C:\Users\user\Projects\HabitFlow-master` as a standalone local git repo; the folder remains a reference snapshot and is not treated as the canonical checkout.
- Created a real upstream-based checkout at `C:\Users\user\Projects\HabitFlow-git`, configured remotes for `upstream = Qwertyil/HabitFlow` and `origin = badinoko/Habbit`, and copied the latest local HabitFlow docs/imports into that checkout.
- Created local git commit `f5d5a3a` (`Bootstrap HabitFlow lab workspace`) in `C:\Users\user\Projects\HabitFlow-git` and force-updated `origin/main` on `badinoko/Habbit` to that history.
- Added tracking tasks for HabitFlow current-state capture and a redesign invariants/artifact-prompt pass.
- No task was marked `DONE`.
- Established a reproducible local baseline in `C:\Users\user\Projects\HabitFlow-git`: installed Poetry dependencies, started `postgres` and `redis`, ran Alembic migrations, launched the app on `http://127.0.0.1:8001`, and confirmed `/healthz/ready` returns OK.
- Captured a full current-state screenshot pack under `docs/screenshots/current_state/` with 10 desktop and 10 mobile full-page screens plus a local README manifest.
- Recorded in startup docs that `HabitFlow-master` remains an accidental standalone repo and should not be used as the canonical workspace in Cursor.
- Recorded the external HabitFlow deployment URL `https://www.amir-vps.ru/` in startup docs for future audits and handoff.
- Stopped the local HabitFlow runtime and `docker-compose` services after confirming they conflicted with Besedka on port `8001`; future local HabitFlow runs should use a different port.
- No task was marked `DONE`.
- Added `docs/README.md` as a navigation index and clarified across `AGENTS.md`, `CODEX.md`, `CLAUDE.md`, `README.md`, `docs/overview.mdc`, `docs/project/startup.md`, and `docs/operations/codex_workflow.md` that `docs/overview.mdc` is product/architecture context while `docs/project/overview.md` is the only task-status dashboard.
- Added active task `HF-009` to track documentation navigation cleanup without rewriting historical records.
- Indexed the user-provided Artifacts handoff file at `docs/artifacts-master-prompt.md` so future sessions can find the prompt from the normal docs entry points.
- No task was marked `DONE`.
- Started `HF-010` implementation from the local artifact pack in `docs/artifacts/` and `docs/cursor-prompt.md`: replaced the shared shell, redesigned core templates, added light/dark theme tokens plus navbar toggle, and switched filter/sort/theme navigation to partial fetch-based updates where feasible.
- Added `docs/theme-system.md` as the SSOT for visual themes and linked it from the main documentation map and overview docs.
- Verified the redesign with `compileall`, full `tests/api_unit`, `tests/api_unit/test_auth_router_html_unit.py`, `tests/api_unit/test_stats_router_unit.py`, `tests/integration/test_stats.py`, and `tests/integration/test_unauthorized_create_redirects.py` with one caveat: repeated integration runs can temporarily fail on this Windows host when Docker cannot immediately rebind PostgreSQL test port `5433`.
- No task was marked `DONE`.

## 2026-03-22

- Applied follow-up navbar/auth polish from user feedback: denser navbar controls, larger typography, stable scrollbar gutter handling, and auth overlay behavior that no longer shifts layout.
- Verified that local HabitFlow infrastructure (`postgres` on `5430`, `redis` on `6370`) can coexist with the user's Besedka stack on this machine.
- Reorganized `docs/` for handoff: moved contracts into `docs/contracts/`, prompts into `docs/prompts/`, and the architecture audit into `docs/reviews/`, then updated references in repo docs and handoff files.
- Added `HF-011` as the next redesign-v2 track for later typography/color iteration in a separate session.
- Prepared the repository for successor handoff with a versioned checkpoint tag plan; no task was marked `DONE`.
- Verified that upstream `https://github.com/Qwertyil/HabitFlow` is fetchable but not writable from this workspace (`403` on dry-run push), then added `HF-012` and `docs/reviews/upstream-proposal-v1.md` so the original author can review our fork through `origin` and tag `v1` without any risk to upstream `master`.
- Started `HF-013` to prune the now-obsolete Besedka import pack after adaptation: the repo keeps local HabitFlow artifacts and prompt history, while assistant handoff docs are being updated to stop referencing `docs/imports/`.
- Shifted the v1 readiness checkpoint to the current `main` tip for handoff continuity, started the explicit v2 planning pass from that base, and opened `HF-014` to define typography and navbar direction before the next implementation wave.
- Added two working review notes for v2 research: `docs/reviews/v2-typography-research.md` captures the confirmed Besedka font roles (`Inter`, `Cormorant Garamond`, `Caveat`), while `docs/reviews/v2-landing-reference-scan.md` compares Besedka with current landing references such as Linear, Raycast, and Stripe before any new code changes.
- Added `docs/reviews/v2-navbar-and-stats-notes.md` to capture direct user feedback for the next wave: navbar items feel too airy vertically, shell type needs clearer role separation, and the stats page may benefit from a sticky left-side section switcher instead of one long vertical sequence.
- Established an archive policy for oversized working docs: added `docs/archive/README.md`, updated `AGENTS.md`, `CODEX.md`, `docs/README.md`, and `docs/operations/codex_workflow.md`, and recorded `HF-015` so future `progress.md` / `overview.md` rotations happen via frozen snapshots instead of ad hoc truncation.
- Consolidated the v2 planning output into `docs/reviews/v2-direction.md` so typography roles, navbar density rules, homepage hierarchy, and stats IA now have one implementation baseline linked from `docs/README.md`, `docs/project/startup.md`, and `docs/project/overview.md`.
- Started the first v2 implementation slice from that direction doc: replaced the old `Unbounded`-heavy display treatment with a calmer `Onest` shell plus `Cormorant Garamond` hero display role, tightened navbar pills and user controls, and updated the homepage hero hierarchy accordingly.
- Added a responsive stats section index to `/stats`: sticky left-side navigation on wider screens with a simpler responsive fallback on narrower widths, while keeping the existing single-route owner-scoped stats model and theme-system contract intact.
- Verified the slice with `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_stats_router_unit.py -q`, `.venv\\Scripts\\pytest.exe tests\\unit\\test_utils.py -q`, and `.venv\\Scripts\\pytest.exe tests\\integration\\test_stats.py::test_get_stats_page_smoke -q`.
- No task was marked `DONE`.
- Applied a direct visual feedback pass for v2 shell/theme UX: simplified the user dropdown to a single `Выход` action, removed redundant theme/stats links from that menu, increased navbar color contrast and saturation, and made the standalone theme switcher visually active instead of monochrome-muted.
- Fixed the `Мои темы` empty-state logic so the page no longer renders both the add-tile and the empty-state block when only the reserved `Все темы` entry exists; the empty state now renders as a dedicated CTA card instead of a cramped follow-up block.
- Verified the feedback pass with `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_themes_router_unit.py -q` and `.venv\\Scripts\\pytest.exe tests\\integration\\test_themes.py::test_get_themes_list_page -q`.
- No task was marked `DONE`.
- Tightened shell spacing after user feedback about the homepage micro-scroll: reduced the top padding below the sticky navbar and aligned the left sticky sidebar offset with the main content so both columns start higher and stop drifting on the first pixels of scroll.
- Reworked `/stats` left navigation from decorative anchor links into a real client-side section switcher: the five left controls now map to `overview`, `tasks`, `habits`, `themes`, and `insights`, toggle the visible panel in place, and update the URL hash without the old page-jump jitter.
- Verified the follow-up with `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_stats_router_unit.py -q` and `.venv\\Scripts\\pytest.exe tests\\integration\\test_stats.py::test_get_stats_page_smoke -q`.
- No task was marked `DONE`.
- Collapsed the separate `/stats` header into a single compact sticky toolbar directly under the main navbar: section switching now lives in one horizontal control row with icons, while the `7 дней / 30 дней` range filter sits in the same toolbar instead of in a duplicated intro block.
- Adjusted the stats tabs implementation so it initializes from the full stats page instead of the old left-column layout; verified in a live browser on `http://127.0.0.1:8010/stats` that `Обзор` is the default visible panel and clicking `Задачи` switches the panel and updates the `#stats-*` hash.
- Synced the stats router/integration smoke tests with the new compact toolbar structure via `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_stats_router_unit.py -q` and `.venv\\Scripts\\pytest.exe tests\\integration\\test_stats.py::test_get_stats_page_smoke -q`.
- No task was marked `DONE`.
- Removed the last sticky jitter from the compact `/stats` toolbar by aligning its sticky offset with the actual shell top padding, and updated the stats tab script so the `7 дней / 30 дней` range links preserve the currently selected `#stats-*` panel instead of snapping back to `Обзор`.
- Compressed the shared desktop form header layout into a left-icon/right-copy row and slightly tightened form gaps/padding so `Создание темы` fits cleanly on Full HD without the previous micro-scroll; mobile forms keep the stacked header layout.
- Verified this pass with `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_stats_router_unit.py -q`, `.venv\\Scripts\\pytest.exe tests\\integration\\test_stats.py::test_get_stats_page_smoke -q`, `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_themes_router_unit.py -q`, plus live browser checks on `http://127.0.0.1:8010/stats` and `http://127.0.0.1:8010/themes/new`.
- No task was marked `DONE`.
- Rebuilt form submission around a single client-side pipeline: generic required checks, habit-specific validators, PUT submission, loading-state control, and error rendering now live in one path instead of three competing submit listeners.
- Form errors now render in a dedicated feedback area above the action buttons and inline next to the failing block/field when possible; this fixes the old “nothing happened” effect on long forms and prevents submit buttons from getting stuck in loading after client-side validation failures.
- Removed the legacy `update.js` submit path, added static asset cache-busting in `base.html` so browsers fetch the new JS/CSS reliably after deploy, and started `docs/reviews/upstream-proposal-v2-draft.md` to accumulate all v2-over-v1.0 deltas for the next upstream handoff.
- Verified the form-fix pass with `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_habits_router_unit.py -q`, `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_tasks_router_unit.py -q`, `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_themes_router_unit.py -q`, `.venv\\Scripts\\pytest.exe tests\\integration\\test_habits.py -q`, `.venv\\Scripts\\pytest.exe tests\\integration\\test_tasks.py -q`, `.venv\\Scripts\\pytest.exe tests\\integration\\test_themes.py -q`, plus live browser checks on `http://127.0.0.1:8010/habits/new` for invalid weekly-days and invalid date-range scenarios.
- No task was marked `DONE`.
- Added overflow protection for long user-generated labels in the active shell: sidebar theme filters, theme pills, theme-card titles, and the navbar user trigger now truncate with ellipsis instead of stretching layout, while the full value remains available via native tooltip (`title`).
- Verified the truncation pass in a live authenticated browser on `http://127.0.0.1:8010/`: the provided local credentials successfully opened the authorized dashboard, and extra-long injected sidebar/user labels stayed within layout bounds without widening the sidebar or navbar controls.
- No task was marked `DONE`.
- Replaced remaining native delete confirms for themes, habits, and tasks with one shared in-app confirmation modal mounted from `base.html` and driven by `HabitFlowUI.confirmAction()`; the modal uses a fixed overlay without scrollbar compensation, so opening it does not shift the navbar or page content sideways.
- Verified the delete-confirm pass with `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_habits_router_unit.py -q`, `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_tasks_router_unit.py -q`, and `.venv\\Scripts\\pytest.exe tests\\api_unit\\test_themes_router_unit.py -q`, plus a live browser check on authenticated `/themes` confirming that clicking `Удалить` opens the custom modal and keeps the navbar at the same left offset before and after opening.
- A broader `tests\\integration\\test_themes.py -q` rerun on this Windows host is still flaky for the old known reason: Docker could not bind PostgreSQL test port `5433`, so that failure was environmental rather than caused by the confirm-modal change.
- Updated the release-prep handoff chain so `docs/reviews/upstream-proposal-v2-draft.md` is now explicitly referenced from startup/overview as the current `v2.0` delta log, including form reliability fixes, delete-modal behavior, and content-safety observations from the old `v1.0` screenshots.
- Followed up on a review-bot finding in `habit_form.js`: date-range validation for weekly habits is now routed to `#ends_on` instead of the weekly-days block, so inline errors reveal the actual failing field rather than the schedule picker; the pending `CLAUDE.md` refresh and `ui.js` DOM-only notification hardening are being captured in the same next commit.
- Refreshed the v2 handoff draft after that follow-up so `docs/reviews/upstream-proposal-v2-draft.md` now also records the corrected `ends_on` error targeting for weekly habits before the current `v2.0` tag is moved to the latest release-ready commit.
- Widened the theme chooser cards in task/habit forms so 24-character theme names stay inside the card without forced ellipsis; the selector now uses a dedicated responsive grid that wraps into new rows instead of compressing all theme cards into the old four-column width.
- No task was marked `DONE`.
